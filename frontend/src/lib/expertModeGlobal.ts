/**
 * Global expert mode utilities for window-level functions
 * @deprecated Expert mode is now managed through settings menu only
 */

/**
 * Initialize global expert mode functions
 * @deprecated No longer needed - expert mode managed through settings
 */
export function initializeExpertModeGlobals(): void {
  // No longer needed - expert mode is managed through settings menu
}

/**
 * Cleanup global expert mode functions
 * @deprecated No longer needed - expert mode managed through settings
 */
export function cleanupExpertModeGlobals(): void {
  // No longer needed - expert mode is managed through settings menu
}